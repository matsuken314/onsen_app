<p><%= @user.name %>さま</p>
<p>パスワードを変更するためのリンクをリクエストされました。以下のリンクからパスワード再設定を行うことができます。</p>

<p><%= link_to t('.action'), edit_user_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('.instruction_2') %></p>
<p><%= t('.instruction_3') %></p>
